import {PosterPipePipe} from './poster-pipe.pipe';

describe('PosterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PosterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
